# Notice/Board Server (Spring Boot)

## Run
1. Update DB credentials in `src/main/resources/application.yml`
2. Create database `notice_board`
3. Run:
   - `mvn spring-boot:run`

## Port
- http://localhost:8082
